# Visitor Analytics Frontend

React dashboard for the FastAPI visitor analytics backend. Polls the API every 4 seconds to show live visitor counts and details.

## Setup

```bash
npm install
npm run dev
```

Set `VITE_API_BASE_URL` to point to the FastAPI service (defaults to `http://localhost:8000`).
